# تحلیل صفحه جزئیات کسب‌وکار در بلد و نشان

## بررسی انجام شده

### روش دسترسی به صفحه جزئیات
- از داده‌های `__NEXT_DATA__` در صفحه اصلی، می‌توان `token` و `urlTitle` را استخراج کرد
- URL صفحه جزئیات: `https://balad.ir/poi/{token}` یا `https://balad.ir/{urlTitle}`

### یافته‌ها

1. **ساختار URL**:
   - Format 1: `https://balad.ir/poi/{token}` (استفاده از token)
   - Format 2: `https://balad.ir/{urlTitle}` (استفاده از urlTitle - ممکن است 404 بدهد)

2. **داده‌های موجود در صفحه اصلی**:
   - `name`: نام کسب‌وکار
   - `category`: دسته‌بندی
   - `token`: شناسه یکتا
   - `urlTitle`: عنوان URL
   - `centerPoint`: مختصات جغرافیایی
   - `rating`: امتیاز و تعداد نظرات

3. **داده‌های موجود در صفحه جزئیات**:
   - نیاز به بررسی بیشتر با مرورگر واقعی
   - احتمالاً phone و email در صفحه جزئیات موجود است
   - ممکن است از API داخلی استفاده شود

### توصیه‌ها برای استخراج کامل

1. **استخراج از صفحه لیست** (فعلی):
   - نام، دسته‌بندی، مختصات، امتیاز
   - لینک به صفحه جزئیات

2. **استخراج از صفحه جزئیات** (نیاز به پیاده‌سازی):
   - باز کردن هر لینک کسب‌وکار
   - استخراج phone و email از DOM یا JSON
   - استخراج آدرس کامل

3. **روش جایگزین**:
   - بررسی Network tab برای API calls
   - استفاده از API داخلی سایت (اگر در دسترس باشد)

### کد پیشنهادی برای استخراج از صفحه جزئیات

```javascript
// در content script
async function scrapeBusinessDetail(businessUrl) {
  // باز کردن صفحه جزئیات در tab جدید یا iframe
  // استخراج phone و email
  // بازگشت به صفحه لیست
}
```

### نکات مهم

- صفحه جزئیات ممکن است نیاز به JavaScript برای render داشته باشد
- باید منتظر لود کامل صفحه بمانیم
- Selectorهای CSS ممکن است نیاز به تنظیم داشته باشند

