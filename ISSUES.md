# مشکلات شناخته شده

این فایل لیست مشکلات و باگ‌های پروژه رو شامل می‌شه. اگر می‌خواهید کمک کنید، اینجا شروع کنید.

## مشکلات بحرانی

### 1. کرش مرورگر بعد از دانلود CSV

**وضعیت:** حل نشده  
**اولویت:** بالا  
**توضیحات:** وقتی روی دکمه "دانلود CSV" کلیک می‌کنید، مرورگر Chrome کرش می‌کنه. این مشکل با فایل‌های کوچک و بزرگ هر دو پیش میاد.

**علائم:**
- مرورگر بعد از کلیک روی دانلود فریز می‌شه
- بعد از چند ثانیه مرورگر کرش می‌کنه
- باید مرورگر رو restart کنید

**راه‌حل‌های امتحان شده:**
- حذف cleanup دستی (removeChild و revokeObjectURL) - کار نکرد
- استفاده از blob URL در content script به جای data URL در service worker - کار نکرد
- اضافه کردن تاخیر در cleanup - کار نکرد
- استفاده از requestAnimationFrame - کار نکرد

**علت:** هنوز مشخص نیست. احتمالاً مشکل از مدیریت memory یا blob URL هست.

**فایل‌های مرتبط:**
- `popup/popup.js` - تابع downloadCSVDirect
- `content/content.js` - تابع downloadCSV
- `background/background.js` - handler دانلود

**کمک نیاز داره:** بله، خیلی. این مهم‌ترین مشکل پروژه هست.

---

### 2. خطای HTTP 418 در حین استخراج

**وضعیت:** تا حدی حل شده  
**اولویت:** بالا  
**توضیحات:** وقتی می‌خواید اطلاعات جزئیات کسب‌وکار (مثل تلفن و ایمیل) رو استخراج کنید، سرور بلد گاهی خطای HTTP 418 می‌ده که یعنی "I'm a teapot" - یعنی احتمالاً تشخیص داده که ربات هستید.

**علائم:**
- استخراج متوقف می‌شه
- خطای HTTP 418 در console
- اطلاعات تماس (تلفن، ایمیل) استخراج نمی‌شه

**راه‌حل فعلی:**
- تاخیر بین درخواست‌ها رو به 3-5 ثانیه افزایش دادم
- مکانیزم retry اضافه کردم (2 بار تلاش مجدد)
- هنوز گاهی پیش میاد

**علت:** تشخیص ربات توسط سرور بلد.ir

**راه‌حل بهتر:** احتمالاً باید تاخیرها رو بیشتر کنیم یا از روش دیگه‌ای استفاده کنیم. یا شاید باید user agent و headerهای دیگه رو تغییر بدیم.

**فایل‌های مرتبط:**
- `background/background.js` - حلقه استخراج جزئیات
- `content/content.js` - تابع getBusinessDetailInfo

---

### 3. قطع ارتباط Content Script

**وضعیت:** تا حدی حل شده  
**اولویت:** متوسط  
**توضیحات:** گاهی ارتباط بین content script و background script قطع می‌شه و پیام‌ها ارسال نمی‌شن.

**علائم:**
- خطای "Could not establish connection. Receiving end does not exist"
- اطلاعات جزئیات استخراج نمی‌شه
- استخراج ادامه پیدا می‌کنه ولی بدون اطلاعات کامل

**راه‌حل فعلی:**
- به جای navigation از fetch API استفاده کردم
- از chrome.scripting.executeScript استفاده کردم
- بازم گاهی پیش میاد

**علت:** احتمالاً service worker terminate می‌شه یا صفحه reload می‌شه

**فایل‌های مرتبط:**
- `background/background.js` - ارسال پیام به content script
- `content/content.js` - دریافت پیام‌ها

---

## مشکلات متوسط

### 4. اطلاعات ناقص استخراج می‌شه

**وضعیت:** حل نشده  
**اولویت:** متوسط  
**توضیحات:** بعضی وقت‌ها اطلاعات کامل استخراج نمی‌شه. مثلاً فقط مختصات میاد و آدرس کامل نمیاد، یا تلفن و ایمیل خالی می‌مونن.

**علائم:**
- فیلدهای خالی در CSV
- فقط مختصات جغرافیایی بدون آدرس کامل
- تلفن و ایمیل خالی

**علت احتمالی:**
- ساختار DOM سایت بلد تغییر کرده
- Selectorهای CSS قدیمی شده
- ساختار JSON در `__NEXT_DATA__` تغییر کرده

**راه‌حل:** باید selectorها و parser JSON رو به‌روز کنیم. اول باید ساختار فعلی سایت رو بررسی کنیم.

**فایل‌های مرتبط:**
- `content/content.js` - توابع استخراج داده

---

### 5. مشکل در pagination

**وضعیت:** حل نشده  
**اولویت:** متوسط  
**توضیحات:** افزونه ممکنه همه صفحات نتایج رو پردازش نکنه و زودتر متوقف بشه.

**علائم:**
- استخراج قبل از تمام شدن صفحات متوقف می‌شه
- دکمه "صفحه بعد" پیدا نمی‌شه
- infinite scroll درست کار نمی‌کنه

**علت احتمالی:**
- Selector دکمه "صفحه بعد" تغییر کرده
- مشکل timing با React/Next.js
- infinite scroll درست handle نمی‌شه

**فایل‌های مرتبط:**
- `content/content.js` - توابع navigation

---

### 6. مشکل memory با داده‌های بزرگ

**وضعیت:** حل نشده  
**اولویت:** متوسط  
**توضیحات:** وقتی تعداد زیادی کسب‌وکار استخراج می‌کنید، ممکنه memory مشکل پیدا کنه.

**علائم:**
- مرورگر کند می‌شه
- استفاده زیاد از memory
- ممکنه کرش کنه

**علت:** همه داده‌ها در memory نگه داشته می‌شن قبل از اینکه CSV ساخته بشه. برای فایل‌های خیلی بزرگ مشکل ایجاد می‌کنه.

**راه‌حل احتمالی:** باید streaming یا chunking اضافه کنیم تا داده‌ها رو به تدریج پردازش کنیم.

**فایل‌های مرتبط:**
- `popup/popup.js` - تولید CSV
- `background/background.js` - ذخیره داده

---

## مشکلات کم‌اهمیت

### 7. مشکلات UI

**وضعیت:** حل نشده  
**اولویت:** پایین  
**توضیحات:** مشکلات کوچک در رابط کاربری.

**مشکلات:**
- progress bar گاهی به‌روز نمی‌شه
- پیام‌های خطا گاهی واضح نیستن
- popup ممکنه در حین عملیات طولانی بسته بشه

**فایل‌های مرتبط:**
- `popup/popup.html`
- `popup/popup.js`
- `popup/popup.css`

---

### 8. محدودیت‌های فیلتر

**وضعیت:** حل نشده  
**اولویت:** پایین  
**توضیحات:** سیستم فیلتر محدودیت‌هایی داره.

**مشکلات:**
- فیلتر case-sensitive هست (ممکنه مشکل ایجاد کنه)
- فقط partial match داره، regex نداره
- فیلتر دسته‌بندی ممکنه همه تغییرات رو match نکنه

**فایل‌های مرتبط:**
- `utils/filters.js`

---

### 9. مدیریت خطا

**وضعیت:** حل نشده  
**اولویت:** پایین  
**توضیحات:** مدیریت خطا کامل نیست.

**مشکلات:**
- بعضی خطاها silently ignore می‌شن
- پیام‌های خطا user-friendly نیستن
- لاگ کردن خطاها نداریم
- خطاهای شبکه درست handle نمی‌شن

---

## مشکلات فنی

### 10. کیفیت کد

**وضعیت:** نیاز به بهبود  
**اولویت:** پایین  
**توضیحات:** کد نیاز به refactoring داره.

**مشکلات:**
- کدهای debug هنوز توی کد هستن
- مدیریت خطا inconsistent هست
- بعضی توابع خیلی طولانی‌ان
- JSDoc comment نداریم
- تست unit نداریم
- توابع utility توی background.js inline شدن (باید توی utils/ باشن)

---

### 11. مستندات

**وضعیت:** ناقص  
**اولویت:** پایین  
**توضیحات:** مستندات کامل نیست.

**مشکلات:**
- README نیاز به به‌روزرسانی داره
- API documentation نداریم
- راهنمای معماری نداریم

---

### 12. تست خودکار نداریم

**وضعیت:** حل نشده  
**اولویت:** متوسط  
**توضیحات:** تست خودکار نداریم، همه چیز دستی تست می‌شه.

**مشکلات:**
- تست unit نداریم
- تست integration نداریم
- CI/CD نداریم
- احتمال باگ‌های regression زیاد هست

---

## مشارکت

اگر می‌خواهید کمک کنید، این مشکلات رو در نظر بگیرید:

1. **کرش بعد از دانلود CSV** - این مهم‌ترین مشکل هست و اولویت اول
2. **خطای HTTP 418** - نیاز به راه‌حل بهتر داره
3. **قطع ارتباط** - باید پایدارتر بشه
4. **استخراج داده** - باید کامل‌تر و دقیق‌تر بشه

راهنمای مشارکت توی CONTRIBUTING.md هست.

---

**نکته:** این پروژه مشکلات زیادی داره و برای استفاده عملی توصیه نمی‌شه. اگر می‌خواهید کمک کنید، خوشحال می‌شم.
